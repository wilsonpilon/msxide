{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run",
      "type": "node-terminal",
      "request": "launch",
      "command": "make run; exit",
      "cwd": "${workspaceFolder}"
    }
  ]
}
